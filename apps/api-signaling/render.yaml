services:
  - type: web
    name: omegle-signaling-server
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm run start:signaling
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
