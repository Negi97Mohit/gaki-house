{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build", "type-check"],
      "outputs": ["dist/**", ".next/**", "!-node_modules"]
    },
    "type-check": {
      "dependsOn": ["^type-check"],
      "outputs": []
    },
    "@gaki/desktop#build": {
      "dependsOn": ["@gaki/web#build", "^build"],
      "outputs": ["dist/**", "release/**"]
    },
    "@gaki/desktop#release": {
      "dependsOn": ["@gaki/web#build", "^build"],
      "outputs": ["dist/**", "release/**"],
      "env": ["GH_TOKEN", "GITHUB_TOKEN"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "clean": {
      "cache": false
    }
  }
}
